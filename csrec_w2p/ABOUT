web2py module for elegans.io recommender

Instructions to use the web2py application:
- download web2py from http://web2py.com
- move the directory csrec_w2p into the applications directory of web2py (or create a symbolic link)
- start web2py
- run the test tool:
    locust -f test0.py RecommTest --host http://127.0.0.1:8000/csrec_w2p/recomm

The Recommender singleton is instantiated in models/db.py, there you can choose wheter use the "in memory"
or the mongo functionalities of the recommender.
e.g.:
    engine = Recommender(log_level=logging.ERROR)
    or
    engine = Recommender(mongo_host="localhost:27017", mongo_db_name="csrec", log_level=logging.ERROR)
